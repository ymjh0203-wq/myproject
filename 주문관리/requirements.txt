# ==========================================================
# 이 프로그램이 필요로 하는 파이썬 패키지 목록
# ----------------------------------------------------------
# 버전을 "=="로 고정해 두었습니다.
# 새 컴퓨터에 설치할 때 더 최신 버전이 깔려서 화면이 깨지거나
# 기능이 동작하지 않는 일을 막기 위한 것입니다.
#
# (이 컴퓨터에서 정상 동작이 확인된 버전들입니다 - 2026-07 기준)
# ==========================================================

streamlit==1.59.2
python-dotenv==1.2.2
requests==2.34.2
pandas==3.0.3
openpyxl==3.1.5
pytest==9.1.1
# 퀵스타 배대지 웹 자동화(설치된 크롬을 몰아서 조작)
playwright==1.61.0
# Supabase(PostgreSQL) 클라우드 DB 연결용. .env에 DATABASE_URL이 있을 때만 사용됩니다.
psycopg2-binary==2.9.10
streamlit-aggrid==1.2.1.post2
