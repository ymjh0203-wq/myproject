# 쿠팡 해외구매대행 상품 발굴 프로그램 — 설계서

> 이 문서는 코드가 아니라 설계입니다. 상세한 버전(다이어그램·표 포함)은 아래 아티팩트 링크에서 볼 수 있습니다.
> https://claude.ai/code/artifact/56ec6624-19c9-4193-990d-c8232888b0bf

## 00. 코딩 전에 확인한 사실

- **확인됨 — 쿠팡파트너스 Open API**: 키워드 검색 가능, 한 키워드당 최대 50개, **검색 API는 시간당 최대 10회 호출 제한**. HMAC 서명 인증. 상품명/가격/이미지/URL/카테고리는 확실히 제공되나, 별점·리뷰수·판매자명·옵션 상세는 응답 포함 여부 **확인 필요**(Phase 0에서 즉시 테스트).
- **확인됨 — 쿠팡 WING 오픈 API**: 공식 API 전체 103개 엔드포인트(Products 22 / Categories 6 / Brands 3 / Shipments&Orders 15 / Returns 7 / Exchanges 4 / Promotions&Coupons 21 / LogisticsInfo 8 / CustomerService 6 / Settlement 2 / CGF 1 / Rocket Growth 8)를 카테고리 단위로 확인. **카탈로그 매칭 검색수를 반환하는 API는 어디에도 없음** — WING UI 전용 내부 기능으로 확인. 이번 프로젝트에서 가장 중요한 결론.
- **확인됨 — 별점·리뷰수도 공식 API에 없음 (신규)**: 파트너스 검색 API 실응답 필드는 `keyword, rank, isRocket, isFreeShipping, productId, productImage, productName, productPrice, productUrl`뿐. WING "상품 요약 정보 조회" API도 `sellerProductId, productId, vendorId, brand, statusName` 등만 제공. 별점/리뷰/판매자/옵션 전부 없음. → 1단계("별점 존재 상품 찾기")도 WING 검색수와 같은 종류의 문제(화면을 봐야만 확인 가능)임이 드러남. 다만 별점은 로그인 없는 공개페이지라 WING보다는 위험도가 낮음.
- **확인됨 — 크롤링 리스크**: 쿠팡 robots.txt는 구글봇 등 일부를 제외한 모든 봇의 크롤링을 금지. 이용약관에도 자동화 도구 사용 제한 조항 존재. 몇 번 반복 요청만으로 차단됐다는 사례 다수 확인. → 대량 상세페이지 스크래핑은 기본 전략으로 채택하지 않음.
- **확인 필요로 남은 항목**: vendorItemId/카탈로그ID의 API 포함 여부, WING 로그인 자동화 시 2차 인증·캡차 발생 조건, WING 카탈로그 매칭 화면이 별점도 함께 보여주는지, 특허청 KIPRIS 상표 검색 API 연동 가능성.

## 01. 요구사항 요약

별점이 존재해 수요가 검증된 쿠팡 상품 중, 중국 해외구매대행으로 실제 소싱 가능한 상품만 골라내는 프로그램. 브랜드/상표 위험, 판매자 고유 조어("효자상품명"), 복잡한 옵션 구성 때문에 진입이 어려운 상품은 제외하고, WING 카탈로그 매칭 검색수로 실수요를 재검증한 뒤 등급(일반 유망상품/S등급)과 함께 저장. 모든 판정에는 근거를 남기고, 제외 상품도 사유와 함께 보관해 사람이 재검토할 수 있어야 함.

## 02. 전체 업무 흐름

별점 존재 상품 탐색 → 해외구매대행 추정(점수) → 브랜드·상표 위험 검사 → 효자상품 의심 검사 → 옵션 복잡도 검사 → 쿠팡 상품번호 확보 → WING 카탈로그 매칭 검색수 확인 → 검색수 판정(0~29 제외 / 30~999 일반 / 1000+ S등급) → URL+분석근거 DB 저장. 각 판정 단계는 서로 독립된 모듈(파이프라인 구조)로 설계.

### 발굴 전략 — 키워드 시드 → 판매자 스토어 확장("몰털이")

① 파트너스 API 키워드 검색으로 해외구매대행 의심 판매자 1명(씨앗)을 찾음 → ② 그 판매자의 `shop.coupang.com/{판매자ID}` 스토어 페이지(로그인 불필요, 별점 필터+판매량순/랭킹순 정렬 내장 확인됨)에서 다른 상품을 대량 수확 → ③ 수확한 상품도 동일 필터 파이프라인을 통과시키고, 새 유망 판매자가 보이면 다시 ②로 반복. 키워드 검색은 콜드스타트용, 실제 수확은 스토어 확장 쪽이 큼. shop.coupang.com은 별도 robots.txt가 없는 것으로 보이나 자동화 허용을 의미하지 않으므로 저빈도 원칙 유지. (확인 필요: 스토어 URL의 판매자ID가 상품 URL의 vendorId와 동일한 값인지)

## 03. 기술 가능성 분석

| 단계 | 판정 | 이유 |
|---|---|---|
| 별점 존재 상품 찾기 | 조건부 가능 | API로 상품 탐색 가능하나 별점 필드 포함 여부 미확인, 시간당 10회 제한으로 속도 매우 느림 |
| 해외구매대행 추정 | 기술 검증 필요 | 신호 대부분이 상세페이지 전용 정보, 크롤링 리스크와 직결 |
| 브랜드/상표 위험 제외 | 구현 가능 | 상품명 텍스트 + 자체 사전만으로 규칙 기반 가능 |
| 효자상품 제외 | 구현 가능 | 상품명+판매자+누적 통계, 초기엔 정확도 낮아 사람 검토 병행 필요 |
| 옵션 복잡도 검사 | 조건부 가능 | 옵션 데이터 API 제공 여부 미확인 |
| 쿠팡 상품번호 확보 | 구현 가능 | URL/응답에 항상 포함 |
| WING 검색수 확인 | 기술 검증 필요(최우선) | 공식 API 근거 없음, 아래 상세 참고 |
| 최종 결과 저장 | 구현 가능 | 단순 로컬 DB 저장 |

### WING 카탈로그 매칭 검색수 상세 분석

| 방식 | 가능성 | 설명 |
|---|---|---|
| 공식 Open API | 근거 없음 | 문서 어디에도 검색수 엔드포인트 없음 |
| 비공개 내부 요청 | 근거 없음 | 리버스엔지니어링 수준, 예고 없이 변경될 위험 |
| 수동 보조 방식 | 가장 안전, **기본 채택** | 프로그램이 번호 목록 제공 → 사람이 WING에서 확인해 값 입력 |
| 브라우저 자동화 | 조건부, 사용자 결정 필요 | 기술적으로 가능하나 로그인/2FA/캡차/세션만료/UI변경/탐지/정책위반 리스크 존재. 캡차 발생 시 우회 시도 금지, 즉시 중단 원칙. **MVP~P4는 미적용, P5에서 옵트인**. |

## 04. 권장 프로그램 구조

**Python + PySide6 + SQLite** 추천 (표 형태 화면 제작 용이, 브라우저 자동화/API 라이브러리 풍부, 로컬 파일 DB로 서버 불필요).

```
coupang_finder/
├─ app/
│  ├─ main.py
│  ├─ ui/  (discovery_view, product_list_view, s_grade_view, excluded_view, detail_dialog, settings_view)
│  ├─ core/
│  │  ├─ collector/ (partners_api_client, discovery_strategy)
│  │  ├─ analyzers/ (overseas_sourcing, brand_risk, hyoja_detector, option_quality, grading)
│  │  ├─ wing/ (search_count_manual, search_count_auto)
│  │  └─ dictionaries/
│  ├─ db/ (models, repository)
│  └─ config/ (settings)
├─ data/ (app.db, dictionaries/)
├─ logs/
└─ requirements.txt
```

화면은 판정 로직을 직접 알지 못하고 결과값만 표시 — 쿠팡 화면이 바뀌어도 나머지 모듈은 영향받지 않도록 분리.

## 05. 데이터베이스 설계

**products** (핵심): id(PK), coupang_product_id(UNIQUE, 중복판정 기준), vendor_item_id, product_name/core_keyword, product_url/category/price, rating/review_count, seller_name/seller_id(FK), overseas_score/basis, brand_risk_level/basis, hyoja_status/prefix, option_count/group_count/quality, search_count/checked_at, grade, status, exclusion_reason, discovered_at/last_checked_at.

**보조 테이블**: sellers, prefix_dictionary(단어 유형/신뢰도/반복통계), search_count_log(시점별 이력), discovery_runs(발굴 세션 기록), discovery_progress(재개용 커서), api_usage_log(호출 제한 추적), settings.

**중복 판정 기준**: `coupang_product_id`를 채택(URL에 포함된 안정적 고유값). vendorItemId는 보조 저장만(재등록 시 변경 가능), URL은 보조 저장(파라미터/리다이렉트 리스크), 상품명+판매자명은 product_id 확보 실패 시 fallback 전용. 재발견 시 upsert로 last_checked_at만 갱신.

## 06. 상품 판정 로직

- **해외구매대행**: 점수제(0~100). 확보 가능 신호(번역투 상품명 등)만 우선 반영, 배송기간/배송비 정책 등 상세페이지 전용 신호는 확인 필요로 분리.
- **브랜드/상표 위험**: 브랜드 사전 대조 → 패턴 탐지("호환"/"정품" 등) → 캐릭터·IP 사전 별도 관리. 안전/주의/제외 3단계, "상표권 침해" 단정 금지. KIPRIS 상표 API 연동은 확인 필요한 고도화 후보.
- **효자상품**: 앞단어 사전 매칭, 브랜드 사전 이관, 동일판매자 3회+ 반복, 타판매자 공통사용 여부, 사전 미등재+데이터부족 시 "확인 필요" 큐. 정상/의심/제외 3단계 + 제외 시 핵심 키워드 별도 저장.
- **옵션 품질**: 옵션명 사전 매칭률, 숫자/모델명/신조어 정규식, 옵션명 길이, 그룹 내 일관성, 가격 편차 → 깔끔/확인필요/번잡.
- **검색수**: 0~29 제외, 30~999 일반 유망상품, 1000+ S등급.

## 07. 화면 설계

1. **상품발굴**: 발굴 시작/중지, 진행상태, 현재 분석 상품, 발견/제외/S등급 수, 실시간 로그
2. **발굴상품**: 상품명·별점·리뷰·검색수·옵션상태·브랜드위험·효자상품여부·해외구매대행가능성·등급·URL 컬럼
3. **S등급**: 발굴상품과 동일 구조, grade=S등급 필터
4. **제외상품**: 사유별 그룹핑, 재검토 후 상태 되돌리기 버튼
5. **상세 분석 팝업**: 원본/핵심 상품명, 상품번호, URL, 이미지, 가격, 별점, 리뷰, 옵션 구조, 각 판정+근거, 검색수, 최종 판정, 제외 사유
6. **설정**: 하루 최대 분석수, 요청 간격, 재검사 주기, 카테고리별 최대 페이지, WING 확인 모드, 자동 제외 임계치

## 08. MVP 범위

포함: 키워드 기반 API 수집, 텍스트 중심 규칙 판정(브랜드/효자상품/옵션), **WING 검색수는 수동 보조 방식**, 4개 화면+상세 팝업, product_id 기준 중복 방지.
제외(이후 단계): 카테고리 전수 순회, 상세페이지 스크래핑이 필요한 정밀 신호, WING 완전 자동화, 스케줄링/무인 실행, 연관상품 확장 탐색.

## 09. 개발 순서

- **Phase 0** WING 검색수 기술검증 — 완료조건: 검색수 확보 방식 확정, 리스크 수준 합의
- **Phase 1** 기본 앱 뼈대 — 완료조건: 빈 화면+DB 연결 동작
- **Phase 2** 상품 수집 — 완료조건: 키워드 입력 시 후보 상품이 중복 없이 DB 적재
- **Phase 3** 필터링 — 완료조건: 후보가 근거와 함께 자동 분류되어 화면 표시
- **Phase 4** 검색수 연동 — 완료조건: 검색수+등급 채워짐, 30 미만 자동 제외
- **Phase 5** 자동화 및 안정화 — 완료조건: 며칠간 무인 실행해도 정책 위반 신호 없이 안정 동작

## 10. 위험요소와 대응책

API 호출 제한(시간당 10회) → 사용량 추적+여유 확보 / 크롤링 차단 → 대량 접근 배제, 저빈도만 / 자동화 불안정 → 셀렉터 외부화+반자동 전환 / 계정 보안 → 평문 저장 금지, 최초 로그인은 사람이 직접 / 브랜드·효자상품·옵션 오판 → "주의/의심/확인필요" 단계 활용, 사람 최종판단 / 데이터 중복 → product_id UNIQUE+upsert / UI·API 변경 → 모듈 분리+설정 외부화 / 계정 정지 리스크 → 최소한의 자동화, 반자동을 기본값으로.

## 11. 구현 전 확인할 질문 (기본값 제안)

1. WING 계정 보유 여부 → 기본값: 있다고 가정
2. 하루 최대 분석수/요청 간격 → 기본값: 하루 100~200개, API 시간당 8회, WING 확인 하루 20~30건
3. WING 완전자동 도입 여부 → 기본값: P4까지 수동 보조, P5에서 사용자 승인 시만
4. 초기 브랜드/효자상품 사전 규모 → 기본값: 유명 브랜드 200개 수동 입력 후 시작
5. 재검사 주기 → 기본값: 일반 30일, S등급 14일
6. 엑셀 내보내기 필요 여부 → 기본값: MVP는 미포함, 이후 추가
